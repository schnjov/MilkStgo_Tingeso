server:
    port: 8090
spring:
    datasource:
        password: theyugi27
        url: jdbc:postgresql://localhost:5432/MilkStgo
        username: postgres
    jpa:
        database: postgresql
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                jdbc:
                    lob:
                        non_contextual_creation: true
        show-sql: true
    sql:
        init:
            platform: postgres